import styled from "@emotion/styled";

export const BaseInputSearch = styled.div`
  height: 45px;
  display: flex;
  flex-direction: row;
  align-items: center;
  margin: auto 10px;
  .MuiInputBase-root {
    padding-left: 0;
    input {
      padding: 8px 12px;
    }
  }
`;
